<%- include('partials/header') %>

<div>
    <ul>
        <% for (const quiz of quizs) { %>
            <li>
                <a href="/quiz/<%= quiz.id %>" class="btn btn-lg btn-block btn-primary text-white"><%= quiz.title %></a>
            </li>
        <% } %>
    </ul>
</div>

<%- include('partials/footer') %>